name: franchiso_vs_code
description: "Meet-Cute Dating App"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.8.0
  firebase_auth: ^5.3.0
  encrypt: ^5.0.3
  cloud_firestore: ^5.4.3
  firebase_storage: ^12.3.2

  image_picker: ^1.0.7
  lottie: ^3.1.0
  flutter_card_swiper: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
